import React from 'react';
import {SafeAreaView} from 'react-native';
import {styles} from './styles';

function AddFit(): React.JSX.Element {
  return <SafeAreaView style={styles.container}></SafeAreaView>;
}

export default AddFit;
